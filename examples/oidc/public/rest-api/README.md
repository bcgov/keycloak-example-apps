# Rest API consuming tokens

This directory contains examples for validating a token that an API receives, for example from a single-page application using a public client.

It can be run in conjunction with the [public react app](../react/) or [public vue app](../vue/) for testing.